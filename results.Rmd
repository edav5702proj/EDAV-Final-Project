# Results

## xysong demo
```{r}
library(ggplot2)
library(dplyr)
library(lubridate)
filename <- "dumbo"
path <- sprintf("Data/data/Magic Kingdom/xysong_python/%s.csv", filename)
df <- read.csv(path)
# Elementary statistics
head(df)
# TODO: (Xiaoyang) whether using detailed time or just date?
df$Datetime <- as.Date(df$Datetime, "%Y-%m-%d %H:%M:%S")
df$Datetime[1]$year
cut(head(df$Datetime), breaks = "month")
# dset <- head(df) %>% dplyr::group_by(month_yr = cut(Datetime, breaks = "1 month") %>% summarise(avg = avg(Waiting.Time))
```